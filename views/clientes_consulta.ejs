<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    .foto-container {
      margin: 20px;
      padding: 20px;
      border: 1px solid #ccc;
    }
    .etiquetas {
      margin-top: 20px;
      padding: 10px;
      border: 1px solid #000;
      background-color: #f0f0f0;
    }
  </style>
</head>
<body>
  <% arrFotos.forEach(foto => { %>
    <div class="foto-container">
      <h2><%= usuario.titulo %></h2>
      <p><%= usuario.descripcion %></p>
      <p>Calificación: <%= ususario.calificacion %></p>
      <img src="<%= foto.ruta %>" alt="<%= foto.titulo %>">
      <div class="etiquetas">
        <h3>Pefil</h3>
        <ul>
          <% Usuario.Perfil.forEach(etiqueta => { %>
            <li><%= Perfil.texto %></li>
          <% }); %>
        </ul>
      </div>
    </div>
  <% }); %>
</body>
</html>